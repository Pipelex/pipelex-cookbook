// >>> pipelex-codegen-stamp >>>
// crate_fingerprint: 5d6d3250d5e2b5f2d7dca659d5aed9c2aeed3b122af951d121efb60c7a05b719
// engine_version: 0.64.0
// projection: types / ts-zod
// options: {}
// content_hash: 097b4e2cba04af8442f6a2f0263066edac74d7980c619c9acaf590712fa49edf
// <<< pipelex-codegen-stamp <<<
// ---------------------------------------------------------------------------
// AUTOGENERATED by Pipelex codegen — DO NOT EDIT.
//
// Typed parse/serialize helpers over the pure `types.ts` schemas. Field keys are
// wire-native (snake_case), so parse/serialize validate the wire payload directly.
//
// projection: types / ts-zod (binder)
// ---------------------------------------------------------------------------
import {
  ArticleContentSchema,
  type ArticleContent,
  ArticleOutlineSchema,
  type ArticleOutline,
  BlogArticleSchema,
  type BlogArticle,
  BlogArticleRequestSchema,
  type BlogArticleRequest,
} from "./types";

/** Parse a wire payload into a validated `ArticleContent`. */
export function parseArticleContent(wire: unknown): ArticleContent {
  return ArticleContentSchema.parse(wire);
}

/** Validate a `ArticleContent` for the wire (keys are already wire-native). */
export function serializeArticleContent(value: ArticleContent): ArticleContent {
  return ArticleContentSchema.parse(value);
}

/** Parse a wire payload into a validated `ArticleOutline`. */
export function parseArticleOutline(wire: unknown): ArticleOutline {
  return ArticleOutlineSchema.parse(wire);
}

/** Validate a `ArticleOutline` for the wire (keys are already wire-native). */
export function serializeArticleOutline(value: ArticleOutline): ArticleOutline {
  return ArticleOutlineSchema.parse(value);
}

/** Parse a wire payload into a validated `BlogArticle`. */
export function parseBlogArticle(wire: unknown): BlogArticle {
  return BlogArticleSchema.parse(wire);
}

/** Validate a `BlogArticle` for the wire (keys are already wire-native). */
export function serializeBlogArticle(value: BlogArticle): BlogArticle {
  return BlogArticleSchema.parse(value);
}

/** Parse a wire payload into a validated `BlogArticleRequest`. */
export function parseBlogArticleRequest(wire: unknown): BlogArticleRequest {
  return BlogArticleRequestSchema.parse(wire);
}

/** Validate a `BlogArticleRequest` for the wire (keys are already wire-native). */
export function serializeBlogArticleRequest(
  value: BlogArticleRequest,
): BlogArticleRequest {
  return BlogArticleRequestSchema.parse(value);
}
