"""
AUTOGENERATED CODE - DO NOT EDIT

If you want to customize this structure:
  1. Copy this file to your own module
  2. Remove the 'structure' or 'refines' declaration from the concept in the PLX file
     and declare it in inline mode (see https://docs.pipelex.com/home/6-build-reliable-ai-workflows/concepts/define_your_concepts/#basic-concept-definition)
  3. Make sure your custom class is importable and registered

To regenerate: pipelex build structures <target_directory>
"""

from typing import List, Literal, Optional

from pipelex.core.stuffs.structured_content import StructuredContent
from pydantic import Field

from examples.c_advanced.gen_expense_data.structures.expense_validator__expense_validation_result import ExpenseValidationResult


class ReportValidationResult(StructuredContent):
    """Complete validation result for an employee expense report"""

    employee_id: str = Field(..., description="Employee ID")
    employee_name: str = Field(..., description="Employee full name")
    expense_results: Optional[List[ExpenseValidationResult]] = Field(default=None, description="Validation results for each expense")
    total_submitted: float = Field(..., description="Total amount submitted")
    total_approved: float = Field(..., description="Total amount approved")
    overall_status: Literal["fully_approved", "partially_approved", "rejected"] = Field(..., description="Overall report status")
