[project]
name = "pipelex-cookbook"
version = "0.19.1"
description = "Worked examples of AI methods, written in MTHDS and run with Pipelex, to read, run and adapt."
authors = [{ name = "Evotis S.A.S.", email = "evotis@pipelex.com" }]
maintainers = [{ name = "Pipelex staff", email = "oss@pipelex.com" }]
license = "MIT"
readme = "README.md"
# The three versions CI's lint and test matrices run, which `dev`'s ruleset requires by name.
requires-python = ">=3.11,<3.14"

dependencies = [
  "httpx>=0.28.1",
  "jinja2>=3.1.6",
  "mthds>=0.8.1",
  "pydantic>=2.13.2",
]

[project.optional-dependencies]
dev = [
  "mypy>=1.11.2",
  "pipelex-tools>=0.3.2",
  "pyright>=1.1.411",
  "pytest>=9.0.1",
  "pytest-mock>=3.14.0",
  "pytest-sugar>=1.0.0",
  "pytest-xdist>= 3.6.1",
  "ruff==0.16.4",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project.urls]
Homepage = "https://pipelex.com"
Repository = "https://github.com/Pipelex/pipelex-cookbook"
Documentation = "https://go.pipelex.com/docs"

[tool.setuptools.packages.find]
include = ["scripts*"]

[tool.uv]
required-version = ">=0.7.2"

[tool.mypy]
check_untyped_defs = true
exclude = [
  "^.*\\.venv/.*$",
  "^scripts/sdk/.*$",
  # The page snippets are written by `make render` and run beside `pipelex-sdk`, which `make check-recipe-types` type-checks them against.
  "^tests/snippets/.*$",
]
mypy_path = "."
packages = ["scripts", "tests"]
plugins = ["pydantic.mypy"]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true

[tool.pyright]
venvPath = "."
venv = ".venv"
pythonVersion = "3.11"
analyzeUnannotatedFunctions = true
deprecateTypingAliases = false
disableBytesTypePromotions = true
enableExperimentalFeatures = false
enableTypeIgnoreComments = true
exclude = [
  "**/__pycache__",
  ".venv",
  ".git",
  "build",
  "dist",
  "**/node_modules",
  "**/.*",
  # Each runs beside `pipelex-sdk`, in an environment of its own, and `make check-recipe-types` type-checks it there.
  "scripts/sdk",
  "tests/snippets",
]
extraPaths = ["./tests"]
include = ["scripts", "tests"]
reportAbstractUsage = "error"
reportArgumentType = "error"
reportAssertAlwaysTrue = "error"
reportAssertTypeFailure = "error"
reportAssignmentType = "error"
reportAttributeAccessIssue = "error"
reportCallInDefaultInitializer = true
reportCallIssue = "error"
reportDeprecated = "error"
reportDuplicateImport = "error"
reportFunctionMemberAccess = "error"
reportGeneralTypeIssues = "error"
reportImplicitOverride = true
reportImplicitStringConcatenation = false
reportImportCycles = true
reportIncompatibleMethodOverride = "error"
reportIncompatibleVariableOverride = "error"
reportIncompleteStub = "error"
reportInconsistentConstructor = "error"
reportInconsistentOverload = "error"
reportIndexIssue = "error"
reportInvalidStringEscapeSequence = "error"
reportInvalidStubStatement = "error"
reportInvalidTypeArguments = "error"
reportInvalidTypeForm = "error"
reportInvalidTypeVarUse = "error"
reportMatchNotExhaustive = "error"
reportMissingImports = "error"
reportMissingModuleSource = "warning"
reportMissingParameterType = "error"
reportMissingSuperCall = "none"
reportMissingTypeArgument = "error"
reportMissingTypeStubs = false
reportNoOverloadImplementation = "error"
reportOperatorIssue = "error"
reportOptionalCall = "error"
reportOptionalContextManager = "error"
reportOptionalIterable = "error"
reportOptionalMemberAccess = "error"
reportOptionalOperand = "error"
reportOptionalSubscript = "error"
reportOverlappingOverload = "error"
reportPossiblyUnboundVariable = "error"
reportPrivateImportUsage = "error"
reportPrivateUsage = "error"
reportPropertyTypeMismatch = true
reportRedeclaration = "error"
reportReturnType = "error"
reportSelfClsParameterName = "error"
reportTypeCommentUsage = "error"
reportTypedDictNotRequiredAccess = "error"
reportUnboundVariable = "error"
reportUndefinedVariable = "error"
reportUninitializedInstanceVariable = "none"
reportUnknownArgumentType = "error"
reportUnknownLambdaType = "error"
reportUnknownMemberType = "error"
reportUnknownParameterType = "error"
reportUnknownVariableType = "error"
reportUnnecessaryCast = "error"
reportUnnecessaryComparison = "error"
reportUnnecessaryContains = "error"
reportUnnecessaryIsInstance = "error"
reportUnnecessaryTypeIgnoreComment = "none"
reportUnsupportedDunderAll = "error"
reportUntypedBaseClass = "error"
reportUntypedClassDecorator = "error"
reportUntypedFunctionDecorator = "error"
reportUntypedNamedTuple = "error"
reportUnusedCallResult = "none"
reportUnusedClass = "error"
reportUnusedCoroutine = "error"
reportUnusedExcept = "error"
reportUnusedExpression = "error"
reportUnusedFunction = "error"
reportUnusedImport = "none"
reportUnusedVariable = "error"
reportWildcardImportFromLibrary = "error"
strictDictionaryInference = true
strictListInference = true
strictParameterNoneValue = true
strictSetInference = true
typeCheckingMode = "strict"


[tool.pytest]
minversion = "9.0"
addopts = ["--import-mode=importlib", "-ra"]
xfail_strict = true
markers = [
  "codex_disabled: tests that should not run in Codex, whose sandbox has no network",
]

[tool.ruff]
exclude = [
  ".git",
  ".github",
  ".mypy_cache",
  ".ruff_cache",
  ".venv",
  ".vscode",
  # A recipe's generated types are written verbatim by codegen and vouched for by their codegen.lock: a formatter that
  # touched them would make the drift check read them as hand-edited.
  "recipes/**/generated",
  # The page snippets are written by `make render`, so a formatter that touched them would make `make check-render` fail.
  "tests/snippets",
]
line-length = 150
target-version = "py311"

[tool.ruff.format]

[tool.ruff.lint.isort]
# A recipe imports the types generated beside it as `generated.<method>`: they are the recipe's own code, not an installed package.
known-first-party = ["generated"]

[tool.ruff.lint]
ignore = ["F401"]
external = ["F401"]
select = [
  "E4",
  "E7",
  "E9",
  "F",
  "A001",
  "A002",
  "A003",
  "RUF008",
  "RUF009",
  "RUF012",
  "RUF013",
  "RUF100",
  "E501",
  "I",
]
