// >>> pipelex-codegen-stamp >>>
// crate_fingerprint: 313fb7e75d1699a9ec3b3abcc838c43a97ada4f33fd872802e1c4174d8aedb95
// engine_version: 0.64.0
// projection: types / ts-zod
// options: {}
// content_hash: 49b6d0b46c9b413765e97b1c24970e56c6dbf13e20faf36b6368ed4baae8b39e
// <<< pipelex-codegen-stamp <<<
// ---------------------------------------------------------------------------
// AUTOGENERATED by Pipelex codegen — DO NOT EDIT.
//
// Typed parse/serialize helpers over the pure `types.ts` schemas. Field keys are
// wire-native (snake_case), so parse/serialize validate the wire payload directly.
//
// projection: types / ts-zod (binder)
// ---------------------------------------------------------------------------
import {
  AttachmentSchema,
  type Attachment,
  ChannelSummarySchema,
  type ChannelSummary,
  DiscordChannelUpdateSchema,
  type DiscordChannelUpdate,
  DiscordMessageSchema,
  type DiscordMessage,
  EmbedSchema,
  type Embed,
  HtmlNewsletterSchema,
  type HtmlNewsletter,
  TextSchema,
  type Text,
} from "./types";

/** Parse a wire payload into a validated `Attachment`. */
export function parseAttachment(wire: unknown): Attachment {
  return AttachmentSchema.parse(wire);
}

/** Validate a `Attachment` for the wire (keys are already wire-native). */
export function serializeAttachment(value: Attachment): Attachment {
  return AttachmentSchema.parse(value);
}

/** Parse a wire payload into a validated `ChannelSummary`. */
export function parseChannelSummary(wire: unknown): ChannelSummary {
  return ChannelSummarySchema.parse(wire);
}

/** Validate a `ChannelSummary` for the wire (keys are already wire-native). */
export function serializeChannelSummary(value: ChannelSummary): ChannelSummary {
  return ChannelSummarySchema.parse(value);
}

/** Parse a wire payload into a validated `DiscordChannelUpdate`. */
export function parseDiscordChannelUpdate(wire: unknown): DiscordChannelUpdate {
  return DiscordChannelUpdateSchema.parse(wire);
}

/** Validate a `DiscordChannelUpdate` for the wire (keys are already wire-native). */
export function serializeDiscordChannelUpdate(
  value: DiscordChannelUpdate,
): DiscordChannelUpdate {
  return DiscordChannelUpdateSchema.parse(value);
}

/** Parse a wire payload into a validated `DiscordMessage`. */
export function parseDiscordMessage(wire: unknown): DiscordMessage {
  return DiscordMessageSchema.parse(wire);
}

/** Validate a `DiscordMessage` for the wire (keys are already wire-native). */
export function serializeDiscordMessage(value: DiscordMessage): DiscordMessage {
  return DiscordMessageSchema.parse(value);
}

/** Parse a wire payload into a validated `Embed`. */
export function parseEmbed(wire: unknown): Embed {
  return EmbedSchema.parse(wire);
}

/** Validate a `Embed` for the wire (keys are already wire-native). */
export function serializeEmbed(value: Embed): Embed {
  return EmbedSchema.parse(value);
}

/** Parse a wire payload into a validated `HtmlNewsletter`. */
export function parseHtmlNewsletter(wire: unknown): HtmlNewsletter {
  return HtmlNewsletterSchema.parse(wire);
}

/** Validate a `HtmlNewsletter` for the wire (keys are already wire-native). */
export function serializeHtmlNewsletter(value: HtmlNewsletter): HtmlNewsletter {
  return HtmlNewsletterSchema.parse(value);
}

/** Parse a wire payload into a validated `Text`. */
export function parseText(wire: unknown): Text {
  return TextSchema.parse(wire);
}

/** Validate a `Text` for the wire (keys are already wire-native). */
export function serializeText(value: Text): Text {
  return TextSchema.parse(value);
}
