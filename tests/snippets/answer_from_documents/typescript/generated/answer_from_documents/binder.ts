// >>> pipelex-codegen-stamp >>>
// crate_fingerprint: 4e6afba12116df0e529461f2a31b0ce18029ac6063d45eadbcd7927a22168be5
// engine_version: 0.64.0
// projection: types / ts-zod
// options: {}
// content_hash: b2fc3bec96eece3b5e54c5ec1b0dd56f711fd13cd44e83a4ebf162cabe31a658
// <<< pipelex-codegen-stamp <<<
// ---------------------------------------------------------------------------
// AUTOGENERATED by Pipelex codegen — DO NOT EDIT.
//
// Typed parse/serialize helpers over the pure `types.ts` schemas. Field keys are
// wire-native (snake_case), so parse/serialize validate the wire payload directly.
//
// projection: types / ts-zod (binder)
// ---------------------------------------------------------------------------
import {
  DocumentAnswerSchema,
  type DocumentAnswer,
  DocumentPassageSchema,
  type DocumentPassage,
  EvidenceAssessmentSchema,
  type EvidenceAssessment,
  QuestionAnalysisSchema,
  type QuestionAnalysis,
  DocumentSchema,
  type Document,
  ImageSchema,
  type Image,
  PageSchema,
  type Page,
  TextSchema,
  type Text,
  TextAndImagesSchema,
  type TextAndImages,
} from "./types";

/** Parse a wire payload into a validated `DocumentAnswer`. */
export function parseDocumentAnswer(wire: unknown): DocumentAnswer {
  return DocumentAnswerSchema.parse(wire);
}

/** Validate a `DocumentAnswer` for the wire (keys are already wire-native). */
export function serializeDocumentAnswer(value: DocumentAnswer): DocumentAnswer {
  return DocumentAnswerSchema.parse(value);
}

/** Parse a wire payload into a validated `DocumentPassage`. */
export function parseDocumentPassage(wire: unknown): DocumentPassage {
  return DocumentPassageSchema.parse(wire);
}

/** Validate a `DocumentPassage` for the wire (keys are already wire-native). */
export function serializeDocumentPassage(
  value: DocumentPassage,
): DocumentPassage {
  return DocumentPassageSchema.parse(value);
}

/** Parse a wire payload into a validated `EvidenceAssessment`. */
export function parseEvidenceAssessment(wire: unknown): EvidenceAssessment {
  return EvidenceAssessmentSchema.parse(wire);
}

/** Validate a `EvidenceAssessment` for the wire (keys are already wire-native). */
export function serializeEvidenceAssessment(
  value: EvidenceAssessment,
): EvidenceAssessment {
  return EvidenceAssessmentSchema.parse(value);
}

/** Parse a wire payload into a validated `QuestionAnalysis`. */
export function parseQuestionAnalysis(wire: unknown): QuestionAnalysis {
  return QuestionAnalysisSchema.parse(wire);
}

/** Validate a `QuestionAnalysis` for the wire (keys are already wire-native). */
export function serializeQuestionAnalysis(
  value: QuestionAnalysis,
): QuestionAnalysis {
  return QuestionAnalysisSchema.parse(value);
}

/** Parse a wire payload into a validated `Document`. */
export function parseDocument(wire: unknown): Document {
  return DocumentSchema.parse(wire);
}

/** Validate a `Document` for the wire (keys are already wire-native). */
export function serializeDocument(value: Document): Document {
  return DocumentSchema.parse(value);
}

/** Parse a wire payload into a validated `Image`. */
export function parseImage(wire: unknown): Image {
  return ImageSchema.parse(wire);
}

/** Validate a `Image` for the wire (keys are already wire-native). */
export function serializeImage(value: Image): Image {
  return ImageSchema.parse(value);
}

/** Parse a wire payload into a validated `Page`. */
export function parsePage(wire: unknown): Page {
  return PageSchema.parse(wire);
}

/** Validate a `Page` for the wire (keys are already wire-native). */
export function serializePage(value: Page): Page {
  return PageSchema.parse(value);
}

/** Parse a wire payload into a validated `Text`. */
export function parseText(wire: unknown): Text {
  return TextSchema.parse(wire);
}

/** Validate a `Text` for the wire (keys are already wire-native). */
export function serializeText(value: Text): Text {
  return TextSchema.parse(value);
}

/** Parse a wire payload into a validated `TextAndImages`. */
export function parseTextAndImages(wire: unknown): TextAndImages {
  return TextAndImagesSchema.parse(wire);
}

/** Validate a `TextAndImages` for the wire (keys are already wire-native). */
export function serializeTextAndImages(value: TextAndImages): TextAndImages {
  return TextAndImagesSchema.parse(value);
}
