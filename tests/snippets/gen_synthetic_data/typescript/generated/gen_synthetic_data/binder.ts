// >>> pipelex-codegen-stamp >>>
// crate_fingerprint: 85fcaeb854a0872a9b243d55afd9e3d6058ec23136ced1bd8db99c09675c47d4
// engine_version: 0.64.0
// projection: types / ts-zod
// options: {}
// content_hash: deb34e5847dfd66ad135866628ca3be7fa0d8e081042d2f568348c246ddd1de6
// <<< pipelex-codegen-stamp <<<
// ---------------------------------------------------------------------------
// AUTOGENERATED by Pipelex codegen — DO NOT EDIT.
//
// Typed parse/serialize helpers over the pure `types.ts` schemas. Field keys are
// wire-native (snake_case), so parse/serialize validate the wire payload directly.
//
// projection: types / ts-zod (binder)
// ---------------------------------------------------------------------------
import {
  NumberSchema,
  type Number,
  TextSchema,
  type Text,
  DataDescriptionSchema,
  type DataDescription,
  NbOfSamplesSchema,
  type NbOfSamples,
  SampleSchema,
  type Sample,
  SampleCaseCharacterizationSchema,
  type SampleCaseCharacterization,
} from "./types";

/** Parse a wire payload into a validated `Number`. */
export function parseNumber(wire: unknown): Number {
  return NumberSchema.parse(wire);
}

/** Validate a `Number` for the wire (keys are already wire-native). */
export function serializeNumber(value: Number): Number {
  return NumberSchema.parse(value);
}

/** Parse a wire payload into a validated `Text`. */
export function parseText(wire: unknown): Text {
  return TextSchema.parse(wire);
}

/** Validate a `Text` for the wire (keys are already wire-native). */
export function serializeText(value: Text): Text {
  return TextSchema.parse(value);
}

/** Parse a wire payload into a validated `DataDescription`. */
export function parseDataDescription(wire: unknown): DataDescription {
  return DataDescriptionSchema.parse(wire);
}

/** Validate a `DataDescription` for the wire (keys are already wire-native). */
export function serializeDataDescription(
  value: DataDescription,
): DataDescription {
  return DataDescriptionSchema.parse(value);
}

/** Parse a wire payload into a validated `NbOfSamples`. */
export function parseNbOfSamples(wire: unknown): NbOfSamples {
  return NbOfSamplesSchema.parse(wire);
}

/** Validate a `NbOfSamples` for the wire (keys are already wire-native). */
export function serializeNbOfSamples(value: NbOfSamples): NbOfSamples {
  return NbOfSamplesSchema.parse(value);
}

/** Parse a wire payload into a validated `Sample`. */
export function parseSample(wire: unknown): Sample {
  return SampleSchema.parse(wire);
}

/** Validate a `Sample` for the wire (keys are already wire-native). */
export function serializeSample(value: Sample): Sample {
  return SampleSchema.parse(value);
}

/** Parse a wire payload into a validated `SampleCaseCharacterization`. */
export function parseSampleCaseCharacterization(
  wire: unknown,
): SampleCaseCharacterization {
  return SampleCaseCharacterizationSchema.parse(wire);
}

/** Validate a `SampleCaseCharacterization` for the wire (keys are already wire-native). */
export function serializeSampleCaseCharacterization(
  value: SampleCaseCharacterization,
): SampleCaseCharacterization {
  return SampleCaseCharacterizationSchema.parse(value);
}
