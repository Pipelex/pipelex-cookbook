// >>> pipelex-codegen-stamp >>>
// crate_fingerprint: cd50d033f4e106ba38ef612573e8d53904e7e165d4038e88af32df8ac4d8889e
// engine_version: 0.64.0
// projection: types / ts-zod
// options: {}
// content_hash: 2378c6d3411fc5cf06da82eae05df5366d5f551d405b68fd0eaf07d18001a74c
// <<< pipelex-codegen-stamp <<<
// ---------------------------------------------------------------------------
// AUTOGENERATED by Pipelex codegen — DO NOT EDIT.
//
// Typed parse/serialize helpers over the pure `types.ts` schemas. Field keys are
// wire-native (snake_case), so parse/serialize validate the wire payload directly.
//
// projection: types / ts-zod (binder)
// ---------------------------------------------------------------------------
import {
  TextSchema,
  type Text,
  FactCheckSchema,
  type FactCheck,
  FormattedReportSchema,
  type FormattedReport,
  ResearchBriefSchema,
  type ResearchBrief,
  ResearchQuestionSchema,
  type ResearchQuestion,
  SourceSummarySchema,
  type SourceSummary,
} from "./types";

/** Parse a wire payload into a validated `Text`. */
export function parseText(wire: unknown): Text {
  return TextSchema.parse(wire);
}

/** Validate a `Text` for the wire (keys are already wire-native). */
export function serializeText(value: Text): Text {
  return TextSchema.parse(value);
}

/** Parse a wire payload into a validated `FactCheck`. */
export function parseFactCheck(wire: unknown): FactCheck {
  return FactCheckSchema.parse(wire);
}

/** Validate a `FactCheck` for the wire (keys are already wire-native). */
export function serializeFactCheck(value: FactCheck): FactCheck {
  return FactCheckSchema.parse(value);
}

/** Parse a wire payload into a validated `FormattedReport`. */
export function parseFormattedReport(wire: unknown): FormattedReport {
  return FormattedReportSchema.parse(wire);
}

/** Validate a `FormattedReport` for the wire (keys are already wire-native). */
export function serializeFormattedReport(
  value: FormattedReport,
): FormattedReport {
  return FormattedReportSchema.parse(value);
}

/** Parse a wire payload into a validated `ResearchBrief`. */
export function parseResearchBrief(wire: unknown): ResearchBrief {
  return ResearchBriefSchema.parse(wire);
}

/** Validate a `ResearchBrief` for the wire (keys are already wire-native). */
export function serializeResearchBrief(value: ResearchBrief): ResearchBrief {
  return ResearchBriefSchema.parse(value);
}

/** Parse a wire payload into a validated `ResearchQuestion`. */
export function parseResearchQuestion(wire: unknown): ResearchQuestion {
  return ResearchQuestionSchema.parse(wire);
}

/** Validate a `ResearchQuestion` for the wire (keys are already wire-native). */
export function serializeResearchQuestion(
  value: ResearchQuestion,
): ResearchQuestion {
  return ResearchQuestionSchema.parse(value);
}

/** Parse a wire payload into a validated `SourceSummary`. */
export function parseSourceSummary(wire: unknown): SourceSummary {
  return SourceSummarySchema.parse(wire);
}

/** Validate a `SourceSummary` for the wire (keys are already wire-native). */
export function serializeSourceSummary(value: SourceSummary): SourceSummary {
  return SourceSummarySchema.parse(value);
}
